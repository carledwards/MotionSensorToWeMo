<Page
    x:Class="MotionSensorToWeMo.MainPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:MotionSensorToWeMo"
    xmlns:wemomodel="using:IoT.WeMo.Model"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">

    <Grid Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">
        <ListView x:Name="wemoDevices" ItemsSource="{x:Bind ViewModel.Devices, Mode=OneWay}"  HorizontalAlignment="Left" Height="183" Margin="102,83,0,0" VerticalAlignment="Top" Width="253" BorderThickness="1" BorderBrush="Black">
            <ListView.ItemTemplate>
                <DataTemplate x:DataType="wemomodel:DeviceModel">
                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="100" />
                            <ColumnDefinition Width="1*" />
                        </Grid.ColumnDefinitions>
                        <TextBlock Text="{x:Bind DeviceName}"/>
                        <Button Grid.Column="1" Content="{x:Bind ButtonTitle, Mode=OneWay}" Click="{x:Bind ToggleState}"/>
                    </Grid>
                </DataTemplate>
            </ListView.ItemTemplate>
        </ListView>
    </Grid>
</Page>
